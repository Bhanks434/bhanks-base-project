/// @title BhanksToken — BHNK utility and governance token
/// @author Bhanks434
/// @notice ERC-20 token deployed on Base

/// @notice Mints new BHNK tokens — only callable by owner
/// @param to Recipient address
/// @param amount Number of tokens (in wei)
function mint(address to, uint256 amount) public onlyOwner {
    _mint(to, amount);
}